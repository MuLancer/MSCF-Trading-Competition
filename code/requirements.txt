requests>=2.31
py_vollib>=1.0.1

# Only the Rotman-provided base scripts need these; vol_strategy.py does not.
pandas>=2.0
numpy>=1.24
tabulate>=0.9          # base scripts call DataFrame.to_markdown()

# For test_news.ipynb
notebook>=7.0
